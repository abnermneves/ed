#include <iostream>
#include <string>
#include "ordenadores.h"
#include "geradores.h"
#include "pilha.h"

using namespace std;

/*int main(int argc, char* argv[]){
  Pilha p = Pilha();
  p.empilha(0, 1);
  p.empilha(2, 1);
  p.empilha(1, 3);
  p.empilha(4, 1);
  p.empilha(0, 6);
  p.empilha(0, 1);
  p.empilha(0, 1);
  p.empilha(9, 8);

  int a, b;
  while(!p.vazia()){
    p.desempilha(&a, &b);
    cout << a << " " << b << endl;
  }
  return 0;
}
*/

int teste(int argc, char* argv[]){

  return 0;
}
